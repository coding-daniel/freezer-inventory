# ------------------------
# Python
# ------------------------
__pycache__/
*.py[cod]
*.so
venv/
*.log
*.tmp

# ------------------------
# Test artifacts and coverage
# ------------------------
pytest_cache/
coverage.*
# No need to duplicate *.log and *.tmp here again

# ------------------------
# Inventory (contains personal data)
# ------------------------
inventory/*.csv

# ------------------------
# Node.js / semantic-release
# ------------------------
node_modules/
# Optional: only ignore if you don't want to track lockfile (common in libraries)
# If using npm ci in CI and want reproducibility, consider tracking it
# Otherwise, this is fine:
package-lock.json

# ------------------------
# macOS & Editor Configs
# ------------------------
.DS_Store
.vscode/
